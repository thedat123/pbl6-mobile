import { SafeAreaView, Text, View } from "react-native";

const TestScreen = () => {
    return (
        <SafeAreaView>
            <Text>Test Screen</Text>
        </SafeAreaView>
    );
};

export default TestScreen;